name: HACS

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  validate:
    runs-on: ubuntu-latest
    name: HACS
    steps:
      - uses: actions/checkout@v5
      - uses: hacs/action@main
        with:
          category: integration
